<!-- index.wxml -->
<view class='index'>
  <view class='header'>
    <view class='header-panel one-px-border'>
      <block wx:for='{{headerTitleName}}' wx:key='{{item.nameID}}'>
        <view data-id='{{item.nameID}}' data-url='{{item.nameUrl}}' data-newstype='{{item.newsType}}' class="header-title {{item.nameID == tapID? 'header-title-active':''}}" bindtap='headerTitleClick' style="position: relative; left: 0rpx; top: -2rpx">
          {{item.name}}
        </view>
      </block>
    </view>
  </view>
  <view class="nav-blank"></view>
  <scroll-view-refresh-load class='scroll-view-refresh-load-component' refreshing="{{refreshing}}" bindpulldownrefresh="onPulldownrefresh_SV">
    <view class='content'>
      <view class='content-list'>
        <block wx:for='{{contentNewsList}}' wx:key='{{item.title}}' wx:if="{{contentNewsList && contentNewsList.length > 0}}">
          <view class='list-item' bindtap='viewDetail'  data-newstitle='{{item.title}}'>
            <view class='list-item-content one-px-border'>
              <view class='list-item-right' style="position: relative; left: 0rpx; top: 2rpx">
                <view class='article-title'>{{item.title}}</view>
              </view>
            </view>
          </view>
        </block>
      </view>
    </view>
    <view class="footer" wx:if="{{showCopyright}}">
      <view class="footer-component">
        <component-copyright></component-copyright>
      </view>
    </view>
  </scroll-view-refresh-load>
</view>